package com.maven_project;/*
    Maven的环境配置:
        Maven下载:
            下载地址: http://maven.apache.org/download.html
        配置Maven环境变量:
            将下载后的压缩包解压缩, 然后把Maven的根目录配置到系统环境变量MAVEN_HOME, 将bin目录配置到path环境变量
            注意: Maven解压后存放的目录中不要含有空格
        检查Maven是否配置成功:
            打开命令行窗口, 输入mvn -v, 安装成功可以显示Maven版本
 */

public class maven_path_configuration {
}
