package com.$4_FaceObject;
/*
    权限修饰符:
    用来控制一个成员能够被访问的范围;
    可以修饰成员变量, 方法, 构造器, 内部类, 不同权限修饰符修饰的成员, 可
    访问的范围不同
    四种权限修饰符, 作用范围由小到大:
        private -> 缺省 -> protected -> public
        1. private:同一个类中可访问
        2. 缺省:同一个类, 同一个包中可访问
        3. protected:同一个类, 同一个包, 不同包下的子类可访问
        4. public:同一个类, 同一个包, 不同包下的子类、无关类均可访问
 */
public class $10_PermissionModifier {
}
